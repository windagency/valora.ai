{
	"name": "@windagency/valora-plugin-memory-vault",
	"version": "1.0.3",
	"description": "Bundled vault memory backend for Valora — biologically-inspired decay, graph retrieval, cosine + Jaccard consolidation. The default `memory.provider`.",
	"keywords": [
		"valora",
		"valora-plugin",
		"memory",
		"vault",
		"embeddings",
		"semantic-recall",
		"typescript"
	],
	"author": "Damien TIVELET <damien@wind-agency.com>",
	"repository": {
		"type": "git",
		"url": "https://github.com/windagency/valora.ai"
	},
	"license": "MIT",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"engines": {
		"node": ">=22.0.0"
	},
	"packageManager": "pnpm@10.19.0",
	"volta": {
		"node": "22.21.0",
		"pnpm": "10.19.0"
	},
	"scripts": {
		"build": "tsc -b && esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:@windagency/valora-plugin-api --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
		"clean": "rm -rf ./dist",
		"lint": "eslint --color",
		"lint:fix": "eslint --color --fix",
		"beautify": "prettier --check \"**/*.+(js|jsx|ts|tsx|json|md|yml|yaml)\"",
		"beautify:fix": "prettier --write \"**/*.+(js|jsx|ts|tsx|json|md|yml|yaml)\"",
		"format": "pnpm beautify:fix && pnpm lint:fix",
		"test": "vitest run",
		"prepublishOnly": "pnpm clean && pnpm build && pnpm test"
	},
	"files": [
		"valora-plugin.json",
		"dist"
	],
	"peerDependencies": {
		"@windagency/valora": ">=0.1.0",
		"@windagency/valora-plugin-api": "workspace:*",
		"@windagency/valora-runtime": "workspace:*"
	},
	"devDependencies": {
		"esbuild": "^0.28.0"
	}
}
